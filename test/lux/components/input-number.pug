---
title: Input numbers
---

each size in ['default', 'sm', 'md', 'lg', 'xl']
  div(class=`test-${size}`)
    span.-text--h2= `Text Input / ${size}`
    table(cellpadding='0', cellspacing='0', width='600px')
      each state in ['initial', 'hover', 'focus', 'disabled']
        tr
          td
            small= `${state}`
          each style in ['default', 'danger']
            td
              .-m--1
                .lux-form__item.-row
                  .lux-number-input(class={[`-${size}`]: size != 'default'})
                    input.lux-input(
                      type='number'
                      class={
                        [`-${style}`]: style != 'default',
                        [`-${state}`]: state != 'disabled'
                      }
                      value='0'
                      disabled=state=='disabled'?'disabled': false
                    )
                    button(tabindex='-1', aria-label='Decrease')
                    button(tabindex='-1', aria-label='Increase')

each size in ['default', 'sm', 'md', 'lg', 'xl']
  div(class=`test-expanded-${size}`)
    span.-text--h2= `Text Input Expanded / ${size}`
    table(cellpadding='0', cellspacing='0', width='600px')
      each state in ['initial', 'hover', 'focus', 'disabled']
        tr
          td
            small= `${state}`
          each style in ['default', 'danger']
            td
              .-m--1
                .lux-form__item.-row
                  .lux-number-input.-expanded(class={[`-${size}`]: size != 'default'})
                    input.lux-input(
                      type='number'
                      class={
                        [`-${style}`]: style != 'default',
                        [`-${state}`]: state != 'disabled'
                      }
                      value='0'
                      disabled=state=='disabled'?'disabled': false
                    )
                    button.lux-button.-icon(tabindex='-1', aria-label='Decrease')
                      .lux-button__content
                        i.lux-icon.icon-minus
                    button.lux-button.-icon(tabindex='-1', aria-label='Increase')
                      .lux-button__content
                        i.lux-icon.icon-plus

each size in ['default', 'sm', 'md', 'lg', 'xl']
  div(class=`test-expanded-svg-${size}`)
    span.-text--h2= `Text Input Expanded SVG / ${size}`
    table(cellpadding='0', cellspacing='0', width='600px')
      each state in ['initial', 'hover', 'focus', 'disabled']
        tr
          td
            small= `${state}`
          each style in ['default', 'danger']
            td
              .-m--1
                .lux-form__item.-row
                  .lux-number-input.-expanded(class={[`-${size}`]: size != 'default'})
                    input.lux-input(
                      type='number'
                      class={
                        [`-${style}`]: style != 'default',
                        [`-${state}`]: state != 'disabled'
                      }
                      value='0'
                      disabled=state=='disabled'?'disabled': false
                    )
                    button.lux-button.-icon(tabindex='-1', aria-label='Decrease')
                      .lux-button__content
                        .lux-icon(class={[`-${size}`]: size != 'default'})
                          svg(aria-hidden="true")
                            use(xlink:href='#icon-minus')
                    button.lux-button.-icon(tabindex='-1', aria-label='Increase')
                      .lux-button__content
                        .lux-icon(class={[`-${size}`]: size != 'default'})
                          svg(aria-hidden="true")
                            use(xlink:href='#icon-plus')
