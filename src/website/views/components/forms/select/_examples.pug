h2 Examples
p.-text
  | To render a select, apply the class <code>lux-input</code> to a <code>select</code>.

h3 Base
.example.-mb--3
  .-p--3
    .lux-form__item(style="max-width:20rem")
      label.lux-label(for="example-base") Label
      select(class="lux-input", id="example-base")
        option(selected disabled hidden) Base Select
        option Option 1
        option Option 2
        option Option 3
  .example-tabs.-pl--2
    ul.lux-tabs#example-select-base
      li.-disabled
        a(href=`#webcomponent-select-base`) Web Component
      li.-active
        a(href=`#html-select-base`) HTML Blueprint
  .lux-tabs-panel#webcomponent-select-base
  .lux-tabs-panel.-active#html-select-base
    :code(lang="html")
      <div class="lux-form__item">
        <label class="lux-label" for="example-base">Label</label>
        <select class="lux-input" id="example-base">
          <option>Base Select</option>
        </select>
      </div>

h3 Disabled
.example.-mb--3
  .-p--3
    .lux-form__item(style="max-width:20rem")
      label.lux-label(for="example-disabled") Label
      select(class="lux-input", id="example-disabled", disabled)
        option Disabled Select
  .example-tabs.-pl--2
    ul.lux-tabs#example-select-disabled
      li.-disabled
        a(href=`#webcomponent-select-disabled`) Web Component
      li.-active
        a(href=`#html-select-disabled`) HTML Blueprint
  .lux-tabs-panel#webcomponent-select-disabled
  .lux-tabs-panel.-active#html-select-disabled
    :code(lang="html")
      <div class="lux-form__item">
        <label class="lux-label" for="example-disabled">Label</label>
        <select class="lux-input" id="example-disabled" disabled>
          <option>Disabled Select</option>
        </select>
      </div>

h3 Invalid
p.-text
  | Selects support the semantic class modifier <code>-danger</code> for indicating
  | an invalid selection.
.example.-mb--3
  .-p--3
    .lux-form__item(style="max-width:20rem")
      label.lux-label(for="example-invalid") Label
      select(class="lux-input -danger", id="example-invalid")
        option Danger Select
      .lux-label.-status.-danger This is an invalid label
  .example-tabs.-pl--2
    ul.lux-tabs#example-select-invalid
      li.-disabled
        a(href=`#webcomponent-select-invalid`) Web Component
      li.-active
        a(href=`#html-select-invalid`) HTML Blueprint
  .lux-tabs-panel#webcomponent-select-invalid
  .lux-tabs-panel.-active#html-select-invalid
    :code(lang="html")
      <div class="lux-form__item">
        <label class="lux-label" for="example-invalid">Label</label>
        <select class="lux-input -danger" id="example-invalid">
          <option>Invalid Select</option>
        </select>
        <div class="lux-label -status -danger">This is an invalid label</div>
      </div>

h3 Sizes
p.-text
  | Selects support the following sizes: <code>-sm</code>, <code>-md</code>,
  |  <code>-lg</code>, <code>-xl</code>. The default size is <code>-md</code>.
.example.-mb--3
  .-p--3
    each size in ['-sm', '-md', '-lg', '-xl']
      div(class=`-p--2`)
        .lux-form__item(style="max-width:20rem")
          label.lux-label(for=`example-size${size}`) Label
          select(class=`lux-input ${size}`, id=`example-size${size}`)
            option(selected disabled hidden)=`${size} Select`
            option Option 1
            option Option 2
            option Option 3
  .example-tabs.-pl--2
    ul.lux-tabs#example-sizes
      li.-disabled
        a(href=`#webcomponent-sizes`) Web Component
      li.-active
        a(href=`#html-sizes`) HTML Blueprint
  .lux-tabs-panel#webcomponent-sizes
  .lux-tabs-panel.-active#html-sizes
    :code(lang='html')
      <!-- sm -->
      <div class="lux-form__item">
        <label class="lux-label" for="example-size-sm">Label</label>
        <select class="lux-input -sm" id="example-size-sm">
          <option>-sm Select</option>
        </select>
      </div>

      <!-- md -->
      <div class="lux-form__item">
        <label class="lux-label" for="example-size-md">Label</label>
        <select class="lux-input -md" id="example-size-md">
          <option>-md Select</option>
        </select>
      </div>

      <!-- lg -->
      <div class="lux-form__item">
        <label class="lux-label" for="example-size-lg">Label</label>
        <select class="lux-input -lg" id="example-size-lg">
          <option>-lg Select</option>
        </select>
      </div>

      <!-- xl -->
      <div class="lux-form__item">
        <label class="lux-label" for="example-size-xl">Label</label>
        <select class="lux-input -xl" id="example-size-xl">
          <option>-xl Select</option>
        </select>
      </div>

h3 Floating labels
p.-text
  | Floating labels are a solution to keep the placeholder visible when no label is attached to the select.
  | Lux only supports floating labels on <code>-lg</code> and <code>-xl</code> selects.

.example.-mb--3
  .-p--3
    each size in ['-lg', '-xl']
      div(class=`lux-col -w--12 -p--2`)
        .lux-input__wrapper.-floating-label(style="max-width:20rem")
          select.lux-input(class=`${size}`, id=`#floating-label-select${size}`)
            option
            option Option 1
            option Option 2
            option Option 3
          label(for=`#floating-label-select${size}`) Placeholder text
  .example-tabs.-pl--2
    ul.lux-tabs#example-label-floating
      li.-disabled
        a(href=`#webcomponent-label-floating`) Web Component
      li.-active
        a(href=`#html-label-floating`) HTML Blueprint
  .lux-tabs-panel#webcomponent-label-floating
  .lux-tabs-panel.-active#html-label-floating
    .lux-tab__description
      span.-text--grey
        | This HTML Blueprint requires JavaScript.
        | You may use your own solution, or use Lux's vanilla JavaScript solution,
        | <a href="../../../getting-started/installation">Lux.js</a>.
    :code(lang='html')
      <div id="floating-label-lg" class="lux-input__wrapper -floating-label">
        <select class="lux-input -lg" id="floating-label-select-lg">
          <option></option>
          <option>Option 1</option>
          <option>Option 2</option>
          <option>Option 3</option>
        </select>
        <label for="floating-label-select-lg">Placeholder text</label>
      </div>

      <div id="floating-label-xl" class="lux-input__wrapper -floating-label">
        <select class="lux-input -xl" id="floating-label-select-xl">
          <option></option>
          <option>Option 1</option>
          <option>Option 2</option>
          <option>Option 3</option>
        </select>
        <label for="floating-label-select-xl">Placeholder text</label>
      </div>

      <script>lux.floatingLabel(document.querySelectorAll('.-floating-label'));</script>

script.
  document.addEventListener(
    'DOMContentLoaded',
    function() {
      lux.tab(document.getElementById('example-select-base'));
      lux.tab(document.getElementById('example-select-disabled'));
      lux.tab(document.getElementById('example-select-invalid'));
      lux.tab(document.getElementById('example-sizes'));
      lux.tab(document.getElementById('example-label-floating'));
      lux.floatingLabel(document.querySelectorAll('.-floating-label'));
    }
  );
