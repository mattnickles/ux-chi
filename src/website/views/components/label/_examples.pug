h2 Examples
h3 Base
.example.-mb--3
  .-p--3
    lux-label(for="input-1") Label
    input#input-1-control.-sr--only
  .example-tabs.-pl--2
    ul.lux-tabs#example-base
      li.-active
        a(href='#webcomponent-base') Web Component
      li
        a(href='#html-base') HTML Blueprint
  #webcomponent-base.lux-tabs-panel.-active
    :code(lang="html")
      <lux-label>Label</lux-label>
  #html-base.lux-tabs-panel
    :code(lang="html")
      <label class="lux-label">Label</label>

h3 Required / Optional

.example.-mb--3
  .-p--3
    lux-label(for="input-2", required) Label
    input#input-2-control.-sr--only
    lux-label(for="input-3", optional) Label
    input#input-3-control.-sr--only
  .example-tabs.-pl--2
    ul.lux-tabs#example-required
      li.-active
        a(href='#webcomponent-required') Web Component
      li
        a(href='#html-required') HTML Blueprint
  #webcomponent-required.lux-tabs-panel.-active
    .lux-tab__description
      | Use attribute <code>required</code> or <code>optional</code> if you need to indicate required / optional fields.
    :code(lang="html")
      <!-- Required -->
      <lux-label required>Label</lux-label>

      <!-- Optional -->
      <lux-label optional>Label</lux-label>

  #html-required.lux-tabs-panel
    :code(lang="html")
      <!-- Required -->
      <label class="lux-label">
        Label
        <abbr class="lux-label__required" title="Required field">*</abbr>
      </label>

      <!-- Optional -->
      <label class="lux-label">
        Label
        <abbr class="lux-label__optional" title="Optional field">(optional)</abbr>
      </label>

h2 Sizes
p.-text Label supports the following sizes: <code>xs</code>, <code>sm</code>, <code>md</code>, <code>lg</code>, <code>xl</code>. The default size is <code>md</code>.
.example.-mb--3
  .-p--3
    lux-label(for="input-4", size='xs') Label
    input#input-4-control.-sr--only
    lux-label(for="input-5", size='sm') Label
    input#input-5-control.-sr--only
    lux-label(for="input-6", size='md') Label
    input#input-6-control.-sr--only
    lux-label(for="input-7", size='lg') Label
    input#input-7-control.-sr--only
    lux-label(for="input-8", size='xl') Label
    input#input-8-control.-sr--only
  .example-tabs.-pl--2
    ul.lux-tabs#example-sizes
      li.-active
        a(href='#webcomponent-sizes') Web Component
      li
        a(href='#html-sizes') HTML Blueprint
  #webcomponent-sizes.lux-tabs-panel.-active
    :code(lang="html")
      <!-- xs -->
      <lux-label size="xs">Label</lux-label>

      <!-- sm -->
      <lux-label size="sm">Label</lux-label>

      <!-- md -->
      <lux-label size="md">Label</lux-label>

      <!-- lg -->
      <lux-label size="lg">Label</lux-label>

      <!-- xl -->
      <lux-label size="xl">Label</lux-label>
  #html-sizes.lux-tabs-panel
    :code(lang="html")
      <!-- xs -->
      <label class="lux-label -xs">Label</label>

      <!-- sm -->
      <label class="lux-label -sm">Label</label>

      <!-- md -->
      <label class="lux-label -md">Label</label>

      <!-- lg -->
      <label class="lux-label -lg">Label</label>

      <!-- xl -->
      <label class="lux-label -xl">Label</label>

h2 Associate label with form elements
p.-text Label is important for accessibility of form elements.

.example.-mb--3
  .-p--3(style="max-width: 17rem;")
    lux-label(for='number-input') Label
    lux-number-input#number-input
    lux-label.-mt--1(for='lux-date-picker') Label
    lux-date-picker#lux-date-picker
  .example-tabs.-pl--2
    ul.lux-tabs#example-associating
      li.-active
        a(href='#webcomponent-associating') Web Component
      li
        a(href='#html-associating') HTML Blueprint
  #webcomponent-associating.lux-tabs-panel.-active
    .lux-tab__description
      | Associate <code>lux-label</code> with supported form-control web components
    :code(lang="html")
      <lux-label for="number-input">Label</lux-label>
      <lux-number-input id="number-input"></lux-number-input>

      <lux-label for="lux-date-picker">Label</lux-label>
      <lux-date-picker id="lux-date-picker"></lux-date-picker>
  #html-associating.lux-tabs-panel
    :code(lang="html")
      <div class="lux-form__item">
        <label for="number-input" class="lux-label">Label</label>
        <div class="lux-number-input">
          <input id="number-input" type="number" class="lux-input" value="0" aria-label="Input Label">
          <button aria-label="Decrease"></button>
          <button aria-label="Increase"></button>
        </div>
      </div>

      <div class="lux-form__item">
        <label class="lux-label" for="datepicker-1">Date</label>
        <div class="lux-input__wrapper -icon--right">
          <input id="datepicker-1" type="text" class="lux-input" placeholder="MM/DD/YYYY">
          <i class="lux-icon icon-date -icon--muted"></i>
        </div>
      </div>

script.
  document.addEventListener(
    'DOMContentLoaded',
    function() {
      lux.tab(document.getElementById('example-base'));
      lux.tab(document.getElementById('example-required'));
      lux.tab(document.getElementById('example-sizes'));
      lux.tab(document.getElementById('example-associating'));
    }
  );
